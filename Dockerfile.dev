# Utviklings-Dockerfile for React-applikasjon
FROM node:alpine

# Sett arbeidskatalogen i containeren
WORKDIR /app

# Kopier package.json og package-lock.json
COPY package*.json ./

# Installer avhengigheter og kjør sikkerhetsskanning
RUN npm install && \
    npm audit --production || echo "Sikkerhetsproblemer funnet, men fortsetter ettersom dette er et utviklingsmiljø"

# Kopier resten av applikasjonskoden
COPY . .

# Eksponer port 3000
EXPOSE 3000

# Start utviklingsserveren
CMD ["npm", "start"]